import { View, Text, Button } from 'react-native';
import React from 'react';

//main dashboard: 
//RECENTLY SAVED POSTS: horziontal scrollable list of recently saved posts? or small grid
//RECENTLY ACCESSED COLLECITOMNS: highlight most recent collection interaction, horziontal scrollable row or smaller tabs
//quick add - button/card to save new post
//Unsorted Posts Workflow: Make the transition to the “Unsorted” collection seamless by automatically 
// opening it when users save without selecting a collection. Add a subtle nudge like “Posts can be organized later!”

const HomePage = ({ navigation }) => {
  return (
    <View>
      <Text>Homepage dashboard here</Text>
    </View>
  );
};

export default HomePage;
