import { View, Text, Button } from 'react-native';
import React from 'react';

const HomePage = ({ navigation }) => {
  return (
    <View>
      <Text>Homepage dashboard here</Text>
    </View>
  );
};

export default HomePage;
